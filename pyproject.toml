[build-system]
requires=["setuptools>=68"]
build-backend="setuptools.build_meta"
[project]
name="langroid-captcha-solver-mcp-tool"
version="0.1.0"
description="How to Add a CAPTCHA Solver MCP Tool to Langroid"
requires-python=">=3.10"
[project.optional-dependencies]
dev=["pytest>=8"]
[tool.pytest.ini_options]
pythonpath=["src"]
